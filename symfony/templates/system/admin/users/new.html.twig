{% extends 'base.html.twig' %}

{% block content %}
    {% form_theme form 'bootstrap_5_layout.html.twig' %}

    <div class="row mb-3">
        <div class="col-lg-8 offset-lg-2 col-md-10 offset-md-1 col-sm-12 mb-3 mb-md-0">
            <div class="card shadow pb-1">
                <div class="card-header">
                    {{ 'user.new_title'|trans }}
                </div>
                <div class="card-body pb-0 mr-2">
                    {{ form_start(form) }}
                    <div class="form-group mb-md-3">
                        {{ form_row(form.name) }}
                    </div>
                    <div class="form-group mb-md-3">
                        {{ form_row(form.surname) }}
                    </div>
{#                    <div class="form-group mb-md-3">#}
{#                        {{ form_label(form.userFacilities) }}#}
{#                        {{ form_widget(form.userFacilities, {'attr': {'class': 'select2', 'multiple': 'multiple'}}) }}#}
{#                        {{ form_errors(form.userFacilities) }}#}
{#                    </div>#}
                    <div class="form-group mb-md-3">
                        {{ form_label(form.roles) }}
                        {{ form_widget(form.roles, {'attr': {'class': 'select2', 'multiple': 'multiple'}}) }}
                        {{ form_errors(form.roles) }}
                    </div>
{#                    <div class="form-group mb-md-3">#}
{#                        {{ form_row(form.pesel) }}#}
{#                    </div>#}
                    <div class="form-group mb-md-3">
                        {{ form_row(form.email) }}
                    </div>
{#                    <div class="form-group mb-md-3">#}
{#                        {{ form_row(form.phone) }}#}
{#                    </div>#}
{#                    <div class="form-group mb-md-4">#}
{#                        {{ form_row(form.ppStatus) }}#}
{#                    </div>#}
{#                    <div class="form-group mb-md-4">#}
{#                        {{ form_row(form.gdpr) }}#}
{#                    </div>#}
                    <div class="form-group mb-md-3">
                        {{ form_row(form.save) }}
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
