<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {#    <link rel="icon" type="image/svg" href="{{ asset('build/images/lama_orange.svg') }}"/>#}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>
<body class="bg-secondary">
<div class="container">
    <div class="row justify-content-center">
        <div class="col-12 col-xl-10">
            <div class="card o-hidden border-0 shadow-lg my-5">
                <div class="card-body p-3 mb-4">
                    <div class="images images-login mb-4">
                        {# TODO: Remake this template #}
{#                                                <a href="https://umed.pl/"><img src="{{ asset('build/images/umed_logo.png') }}"></a>#}
{#                                                <a href="https://biostat.umed.pl/"><img src="{{ asset('build/images/btm_logo.png') }}"></a>#}
                    </div>
                    {% include 'system/parts/flashes.html.twig' %}
                    {% block content %}{% endblock %}
                    <div class="d-flex justify-content-between align-items-baseline">
                        <div class="col-4">
                            <div>
                                <p>
                                    <b>Kontakt w sprawach merytorycznych</b><br>
                                    e-mail: <a href="#">mail@1.pl</a><br>
                                    tel. 42 123 45 67
                                </p>
                            </div>
                        </div>
                        <div class="images-login-main col-4 text-center my-3 mx-3">
                            <img src="{{ asset('build/images/lama_white_blue_circle.svg') }}" alt="Project logo">
                        </div>
                        <div class="col-4">
                            <p>
                                <b>Kontakt w sprawach technicznych</b><br>
                                e-mail: <a href="#">mail@2.pl</a><br>
                                tel. 42 987 65 43
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% block javascripts %}
    <script src="https://kit.fontawesome.com/{{ get_env('FONT_AWESOME_KIT') }}.js" crossorigin="anonymous"></script>
    <script>
        {#let cookiePath = '{{ path('main_page_gdpr') }}';#}
        let httpHost = '{{ app.environment == 'dev' ? 'localhost' : app.request.httpHost }}';
        let locale = '{{ app.request.locale }}';
    </script>
    {{ encore_entry_script_tags('app') }}
    {#    {{ encore_entry_script_tags('cookiebox') }}#}
{% endblock %}
</body>
</html>
